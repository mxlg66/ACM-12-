#include <stdio.h>
using namespace std;

int main()
{
    int n,m;
    while(scanf("%d%d",&n,&m) == 2)
    {
        if(n==0 && m==0)break;
        if(n&1 && m&1)printf("What a pity!\n");
        else printf("Wonderful!\n");
    }
    return 0;
}
