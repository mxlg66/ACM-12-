#include<stdio.h>
#include<string.h>
#include<stdlib.h>
#include<algorithm>
#include<string.h>
using namespace std; 
int main() {
	int m,a;
	while (scanf("%d", &m), m) {
		int sum = 0;
		while (m--) {
			scanf("%d", &a);
			sum ^= a;
		}
		if (sum) printf("Rabbit Win!\n");
		else printf("Grass Win!\n");
	}
}
